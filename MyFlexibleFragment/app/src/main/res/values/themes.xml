<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyFlexibleFragment" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_variant</item>
        <item name="colorOnPrimary">@color/on_primary</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_variant</item>
        <item name="colorOnSecondary">@color/on_secondary</item>

        <!-- Background color. -->
        <item name="android:windowBackground">@color/background</item>

        <!-- Surface color (e.g., cards, dialogs). -->
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/on_surface</item>

        <!-- Error color. -->
        <item name="colorError">@color/error</item>

        <!-- Text and icon colors. -->
        <item name="colorOnBackground">@color/on_background</item>
        <item name="colorOnError">@color/on_error</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <!-- Customize other attributes here if needed. -->
    </style>

    <!-- Application theme that inherits from the base theme. -->
    <style name="Theme.MyFlexibleFragment" parent="Base.Theme.MyFlexibleFragment" />
</resources>